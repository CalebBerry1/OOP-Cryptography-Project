// main function that calls the other algorithm classes
// this may be redundant with the CipherFactory but this can be used for testing at least

#include "XOR.h"

int main() {
    XOR objectXOR;

    objectXOR.mainXOR();

    return 0;
}