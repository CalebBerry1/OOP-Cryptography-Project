import Gui
import QtQuick
import QtQuick.Controls
import QtQuick.Window

Rectangle {
    id: homePage
    anchors.fill: parent
    color: 'blue'

    Label {
        anchors.centerIn: parent
        text: "Home Page"
    }
}